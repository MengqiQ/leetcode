public class CourseSchedule207 {
    public boolean canFinish(int numCourses, int[][] prerequisites) {

    }

    private boolean bfs(int num, int[][] prerequisites) {
        boolean[] visited;
        for (int i = 0; i < prerequisites.length; i ++) {
            visited[i] =
        }
        while (num != 0) {

        }
    }
}
